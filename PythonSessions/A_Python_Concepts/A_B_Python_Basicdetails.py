Python data types:
Number: int , float : 10, 20, 30,   10.5 20.5
String: Str         : "pyhon", 'python'
Boolean: bool       : Treu , False

Type of variables:
1. Local variables
2. Global variables

Functions:
def disp1():
    print("Good morning")
def disp2(name):
    print("Good evening", name)

Observation1: Inside the function, once we have used the gloabl variable, inside the function, it is not
possible to create a local variable. We will get unbounded loacl varaibles.

Observation2: %f and %g --> %g only display only 6 Characters. where as %f will display
6 digits after "."

Python identifiers:
1. a-z, A-Z and 0-9,_ Should not start with numbers and should not allow special Characters
2. case sensitive
3. no lenth limit
4. keywords are not allowed a identifiers.

Global keyword: if we want to decleare the global varible we can use 'global' keyword.

nonlocal keyword: if we want to access the parent method variable in chaild method
we can use nonlocal keyword.

Python functions:
1. Predefined functions
2. User-defined functions

Functions arguments:
1. Default arguments
2. Required arguments
3. keyword argumnents
4. Variable arguments
Observation3: Once the default argument is started, then the following arguments also default arguments.
Observation4: Once the keyword argument is started, then the following arguments also keyword aruguments.

Inner funcations: Declare a functions with in the functions.
